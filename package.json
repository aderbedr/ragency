{
  "name": "ragency",
  "version": "0.0.1",
  "engines": {
    "node": ">=7.5.0",
    "npm": ">=4.1.2"
  },
  "private": true,
  "description": "Rage on.",
  "dependencies": {
    "express": "^4.14.1",
    "knex": "^0.12.6",
    "pg": "^6.1.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "postinstall": "if [ \"$NODE_ENV\" = production ]; then cd ui/ && npm install --only=dev && npm install && npm run build; fi"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/deonna/ragency.git"
  },
  "bugs": {
    "url": "https://github.com/deonna/ragency/issues"
  },
  "homepage": "./"
}
